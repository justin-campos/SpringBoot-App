sever:
  port: 1010
loggin:
  level:
    org:
      springframework:
        jdbc:
          datasource:
            ScriptUtils: "debug"
spring:
  h2:
    console:
      enabled: true
  datasource:
    url: "jdbc:h2:mem:testdb"
  jpa:
    hibernate:
      ddl-auto: "none"

  data:
    rest:
      base-path: "api/rest"


---
